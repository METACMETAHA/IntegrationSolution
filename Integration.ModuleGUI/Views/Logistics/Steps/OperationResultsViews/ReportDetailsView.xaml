<UserControl x:Class="Integration.ModuleGUI.Views.Logistics.Steps.OperationResultsViews.ReportDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:common="clr-namespace:IntegrationSolution.Common.Helpers;assembly=IntegrationSolution.Common"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls">
    <StackPanel Margin="10">

        <Border Padding="0 5">
            <Grid>
                <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, 
                 RelativeSource={RelativeSource TemplatedParent}}"/>
                <!--<TextBlock

                Text="&#xf002;" FontSize="14" Margin="0 0" 

				VerticalAlignment="Center"

				HorizontalAlignment="Right"/>-->
            </Grid>
        </Border>

        <common:FilteredListView ItemsSource="{Binding SAPs}" FilterPredicate="{Binding StateNumberVehiclesFilter}"/>

        <TextBlock Text="Next"/>
        <TextBlock Text="{Binding Test}"/>
        <ListBox ItemsSource="{Binding SAPs, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

    </StackPanel>
</UserControl>
