<UserControl x:Class="Integration.ModuleGUI.Views.OperationResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Integration.ModuleGUI.Views"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:views="clr-namespace:Integration.ModuleGUI.Views.Logistics.Steps.OperationResultsViews"
             xmlns:converters="clr-namespace:IntegrationSolution.Common.Converters;assembly=IntegrationSolution.Common"
             xmlns:partialViews="clr-namespace:Integration.PartialViews.Views;assembly=Integration.PartialViews"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:wnd="http://metro.mahapps.com/winfx/xaml/simplechildwindow"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <converters:PercentDifferenceConverter x:Key="PercentConverter"/>
        <Style TargetType="{x:Type TextBlock}">
            <Style.Setters>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style.Setters>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <!--<wnd:ChildWindow 
            Grid.ColumnSpan="2" Title="Подробно"
            IsOpen="{Binding IsOpenChildPopup}"
            IsModal="False" ShowTitleBar="True" 
            CloseByEscape="True"
            ShowCloseButton="True" Padding="30"
            HorizontalContentAlignment="Center"
            VerticalContentAlignment="Center">
           

        </wnd:ChildWindow>-->

        <!--Tab with Simple Data-->
        <Controls:MetroAnimatedSingleRowTabControl>
            <Controls:MetroTabItem Header="Сводка">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <views:ReportSimpleView Height="Auto"/>
                </ScrollViewer>
            </Controls:MetroTabItem>

            <Controls:MetroTabItem Header="Сравнение">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="3*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel x:Name="panel" Height="500" ScrollViewer.CanContentScroll="True">
                            <TextBlock Margin="10 0 10 0" Text="Список ТС" Style="{DynamicResource MedHeaderBold}"/>
                            <ListBox Margin="10"
                                 Height="{Binding ElementName=panel, Path=Height}"
                                 Width="{Binding ElementName=panel, Path=ActualWidth}"
                                 ItemsSource="{Binding ModuleData.SimpleDataForReport}" 
                                 SelectedItem="{Binding SelectedVehicleInfoInMilesChart}"
                                 ScrollViewer.CanContentScroll="True"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="SelectionChanged">
                                        <i:InvokeCommandAction Command="{Binding OnCarChangedCmd}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="5 2" ToolTip="{Binding UnitModel}">
                                            <Grid.RowDefinitions>
                                                <RowDefinition/>
                                                <RowDefinition/>
                                                <RowDefinition/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.RowSpan="3" TextWrapping="WrapWithOverflow" Text="{Binding StateNumber}" 
                                                       FontSize="17" FontStretch="UltraCondensed" FontWeight="Light"
                                                       HorizontalAlignment="Left"/>

                                            <TextBlock Grid.Column="1" Text="{Binding PercentDifference, Converter={StaticResource PercentConverter}}" HorizontalAlignment="Right"/>

                                            <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Type}" HorizontalAlignment="Right"/>

                                            <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding StructureName}" HorizontalAlignment="Right"/>

                                        </Grid>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding PercentDifference}" Value="{x:Null}">
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                        
                        <partialViews:PredictionChartView Grid.Column="1" ScrollViewer.CanContentScroll="True" DataContext="{Binding PredictionChartContext}"/>
                        
                    </Grid>
                </ScrollViewer>
            </Controls:MetroTabItem>
        </Controls:MetroAnimatedSingleRowTabControl>

        <partialViews:GridConfigurationView Grid.Column="1"/>
    </Grid>
</UserControl>
