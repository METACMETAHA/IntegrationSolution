<UserControl x:Class="Integration.Infrastructure.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Integration.Infrastructure.Views">
    <!--Header-->
    <Grid Grid.Row="0" Background="{StaticResource HeaderBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Style="{StaticResource NormalHeaderInverse}" Text="Add Multi Site"/>
        <!--<Image Grid.Column="1" Source="{StaticResource AfconLogo}" HorizontalAlignment="Right" Height="30" Margin="20"/>-->

    </Grid>
</UserControl>
